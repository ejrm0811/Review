

1. 아두이노와 각종 장치 연결(LED, 센서, 모터 등)
2. 아두이노 개발환경(IDE) 실행 - SKETCH
3. 소스코드 작성
4. 컴파일 및 업로드
    컴파일 VS 빌드
        컴파일 : 소스의 문법을 분석해 기계어로 번역
        빌드 : 번역된 것과 그외 잡다한걸 모아서 실행 가능한 파일로 만듬.
5. 아두이노 동작 확인
6. 디버깅

- 아두이노 구조
    
    ※ 주의
    상부 디지털 입출력 핀/PWM 핀
    하부 아날로그 입출력 핀

- 아두이노 

    /*  */ : 주석부분, 협업시 유용하게 사용될 수 있음. 설계 내용 등 컴파일을 원치 않는 부분 처리
    ch341(짭두이노)
    아두이노 컴포트(에러에 관한 내용)
        아두이노 ftdi, ch341 driver(WINDOWN 7)) 보드의 제조사 별로 다르다.
        제조사가 기본 부트로더 펌웨어를 어떤 것을 올려놓느냐에 따라 달라진다.
        두 드라이버를 모두 가지고 있으면 모든 보드와 호환된다고 생각할 수 있다.
        D2XX,D3XX(대형 보드에 많이 이용)


    -> 용어를 익히고 검색해서 사용하면 된다.(용어는 반드시 외울 것)
        (크롬에 자신이 사용하고자 하는 '언어+java' 연산자 를 입력한다.)
    
    http://arduino.cc/en/Reference/Homepage 전부 읽어보면 코딩 스스로 가능
    
    Code
        문법
            구조(외울 필요는 없다 - 검색)
            
                    - 기본 구조문
                    - 제어 구조문
                    - 기타 구조문
                    - 수 연산자(사칙연산)
                    - 비교 연산자(크다, 작다, 같다, 다르다)
                    - 논리 연산자(and, or, not)
                    - 포인트 연산자 -> 내부에서 자동으로 처리
                    - 비트 연산자(비트를 가지고 노는 것 이다)
                    - 합성 연산자

                변수, 상수, 변수, 데이터 타입, 형 변환 (에디터 공통)
                함수
                라이브러리 ☆

            1. 들여쓰기 필수(Manner가 사람을 만든다~) TAB - sketch경우는 2칸/C&자바 - 4칸/python에서는 필수
            2. /* ? */ -> ?는 전부 주석처리
            3. ';' -> 여기까지가 한 줄이야.(컴퓨터 문장의 마지막을 정의하는 것)
            4. 암기.
                - 상수와 변수

                    상수 : 변하지 않는 값(Header constant)
                    변수 : 변하는 데이터, 데이터를 저장하는 공간.

                - 선언과 초기화

                    선언(데이터형,변수이름 = 초기화)
                        const int led=13; 상수
                        int led= 13; 변수

                    초기화
                        상수는 선언하면서 값을 주어짐
                        변수는 선언하면서 초기화해도 되고 프로그램 중간에 넣어도 된다.

                - 상수/변수 이름 짓기
                    영문 대소문자. 숫자, '_'만 사용함. 대소문자 구분됨
                    특수문자 안됨
                    띄어쓰기가 있으면 안됨
                    숫자가 맨 앞에 오는 것은 안됨
                    실제변수의 기능에 따라 이름을 작명 


            if 문 {}로 구분.
            for 문 {실행영역}


            변수 - 컴퓨터 언어에서 무언가를 담는데 사용되는 것(숫자, 문자)

            함수 - <전달 받은 변수 = 전달인자> 를 가공 또는 미리 작성한 일을 처리하는 곳 -> Ouput 이 있을 수도 있고 없을 수도 있다.

                1. 구조
                    "반환값" = "함수이름"("전달인자1","전달인자2",,,,)
                    ex) 
                        digitalWrite(pin,HIGH)

        설계
            1. 프로그램 설명부, 프로그램 동작설명, 제작자, 라이선스, 생성연월일/버전, 등 정보 부분 (head)
            2. 설계주석
            3. 변수 및 함수 정의부
                int led = 13;
                라이브러리
            
            절차 언어(setup, loop) 특성
            4. setup <프로그램 시작할 때 한번만 실행되는 코드>
                
                입출력 핀의 초기값
                시리얼 포트 설정
                변수 초기값
                
            5. loop < 메인실행 함수, 반복 실행 됨 >
                
                주 실행 프로그램 부분
                무한 반복 됨

            6. user library (직접 만들수도 있고 남의 것 가져다 사용 가능)
        
        Serial Monitor -> Logic check

        Library download ☆

        아두이노는 하드웨어 부터 구성한다. -> 코딩

            - led 가장 긴 핀은 전원 -> power 3.3v
            - 나머지 선은 무작위로 꼽은 후 코드로 찾는다.

        2018.10.02 센서값 받기-> 값에 따라 변형.

            


        





        








